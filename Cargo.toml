[package]
name = "cellon"
version = "1.4.0"
edition = "2021"
description = "Ultra-fast Rust-powered Python async web framework"
license = "MIT"
repository = "https://github.com/jagadeesh32/cello.git"
keywords = ["web", "framework", "async", "rust", "python"]
categories = ["web-programming::http-server", "asynchronous"]

[lib]
name = "cello"
crate-type = ["cdylib"]

[dependencies]
# Python bindings (minimum Python 3.12)
pyo3 = { version = "0.20", features = ["extension-module", "abi3-py312"] }

# Tokio-backed Python async event loop (drives async def handlers via Tokio, not asyncio)
pyo3-asyncio = { version = "0.20", features = ["tokio-runtime"] }

# Async runtime
tokio = { version = "1", features = ["full"] }

# Async HTTP client (Rust-native, no GIL during I/O)
reqwest = { version = "0.11", features = ["rustls-tls", "gzip"], default-features = false }

# HTTP server
hyper = { version = "1", features = ["full"] }
hyper-util = { version = "0.1", features = ["full"] }
http-body-util = "0.1"

# Socket configuration (SO_REUSEPORT for multi-process)
socket2 = { version = "0.5", features = ["all"] }

# WebSocket support
tokio-tungstenite = "0.21"
futures-util = "0.3"

# Routing
matchit = "0.7"

# JSON - using simd-json with serde fallback
simd-json = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Arena allocator for zero-copy
bumpalo = { version = "3", features = ["collections"] }

# Utilities
bytes = "1"
parking_lot = "0.12"
urlencoding = "2"
mime = "0.3"
mime_guess = "2"
async-trait = "0.1"
pin-project-lite = "0.2"
thiserror = "1"
tracing = "0.1"
futures-core = "0.3"

# Multipart form parsing
multer = "3"

# Compression
flate2 = "1"

# MiniJinja - Jinja2-compatible template engine (optional middleware)
minijinja = { version = "2", features = ["loader", "builtins", "json"] }

# === Enterprise Features ===

# JWT Authentication
jsonwebtoken = "9"

# Concurrent HashMap for rate limiting
dashmap = "5"

# XML serialization
quick-xml = { version = "0.31", features = ["serialize"] }

# HTTP/3 (QUIC) and TLS
quinn = "0.10"
tokio-rustls = "0.25"
rustls = { version = "0.22", default-features = false, features = ["ring"] }
rustls-pemfile = "2"

# Cancellation and utilities
tokio-util = { version = "0.7", features = ["rt"] }

# Session and security
uuid = { version = "1", features = ["v4"] }
rand = "0.8"
base64 = "0.21"
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
crc32fast = "1"
subtle = "2"  # Constant-time comparison for timing attack prevention

# System info
num_cpus = "1"

# Route constraints
regex = "1"

# HTTP/2 support
h2 = "0.4"

# Prometheus metrics
# Note: "process" feature uses /proc on Linux for process metrics.
# It's a no-op on non-Linux but compiles fine cross-platform.
prometheus = { version = "0.13", features = ["process"] }

# Date/time handling for exception timestamps
chrono = { version = "0.4", features = ["serde"] }

# === Enterprise Features v0.7.0+ / v0.8.0 Data Layer ===

# OpenTelemetry - Distributed Tracing & Observability
opentelemetry = { version = "0.21", features = ["trace", "metrics"] }
opentelemetry-otlp = { version = "0.14", features = ["tonic", "metrics"] }
opentelemetry_sdk = { version = "0.21", features = ["rt-tokio", "trace", "metrics"] }
tracing-opentelemetry = "0.22"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Database Connection Pooling (native Postgres pool — first-class since v1.3.0)
deadpool = "0.10"
deadpool-postgres = "0.12"
tokio-postgres = { version = "0.7", features = [
    "with-chrono-0_4",
    "with-uuid-1",
    "with-serde_json-1",
] }

# Native async Redis client (redis crate — first-class since v1.3.0)
# rustls TLS features enable `rediss://` URLs (matches the project's rustls stack).
redis = { version = "0.25", features = [
    "tokio-comp",
    "connection-manager",
    "tls-rustls",
    "tls-rustls-webpki-roots",
    "tokio-rustls-comp",
] }

# GraphQL Support
async-graphql = { version = "7", optional = true }
async-graphql-value = { version = "7", optional = true }

# gRPC Support
tonic = { version = "0.11", optional = true }
prost = { version = "0.12", optional = true }

# Additional utilities for enterprise features
sysinfo = "0.30"  # System metrics for health checks
hostname = "0.3"  # Hostname for tracing

[features]
default = []
# `postgres`/`tokio-postgres` are always compiled now; kept as an empty alias so
# existing `--features postgres` / `full` invocations keep working.
postgres = []
graphql = ["async-graphql", "async-graphql-value"]
grpc = ["tonic", "prost"]
full = ["postgres", "graphql", "grpc"]

[dev-dependencies]
tokio-test = "0.4"
tempfile = "3"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
strip = true
panic = "abort"
# PERF: Disable overflow checks in release for maximum speed
overflow-checks = false

[profile.bench]
lto = "fat"
opt-level = 3
codegen-units = 1
